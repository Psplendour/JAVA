package staticPoc;
//Another example of a static method that performs a normal calculation
public class Calculate {
      static int cube(int length)
      {
    	  return(length*length*length);
      }
}
